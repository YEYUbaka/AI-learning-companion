# 🚀 快速启动指南

## 完整启动流程

### 步骤 1：启动后端

打开 **第一个** PowerShell 窗口：

```powershell
cd "F:\Cursor projects\Web\backend"
.\启动.bat
```

等待看到：
```
Backend service starting...
URL: http://127.0.0.1:8000
```

### 步骤 2：启动前端

打开 **第二个** PowerShell 窗口（重要：新开一个窗口）：

```powershell
# 重新加载环境变量（Node.js）
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

# 进入前端目录
cd "F:\Cursor projects\Web\frontend"

# 启动服务
npm run dev
```

等待看到：
```
➜  Local:   http://localhost:5173/
```

## 🌐 访问地址

- **前端应用**：http://localhost:5173
- **后端API**：http://127.0.0.1:8000/docs

## 📝 如果 npm 找不到

在 PowerShel 中运行（**只需一次**）：

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

然后就可以使用 npm 命令了。

## ✅ 快捷方式

我已经创建了简化版的启动脚本：
- **后端**：`backend/启动.bat` （避免编码问题）
- **前端**：使用上面的 PowerShell 命令

## 🎯 测试流程

1. 启动后端（终端1）
2. 启动前端（终端2）
3. 访问 http://localhost:5173
4. 点击"注册" → 填写信息 → 注册
5. 点击"AI助手" → 输入问题 → 查看回答

## ⚠️ 重要提示

- 必须同时运行**两个服务**（前后端）
- 使用**两个不同的**终端窗口
- 如果 npm 找不到，运行上面的环境变量刷新命令

