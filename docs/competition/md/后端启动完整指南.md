# 🚀 后端启动完整指南

## 📋 第一步：配置环境变量

### 方法1：从模板创建（推荐）

如果存在 `.env.template` 文件：

```powershell
cd "F:\Cursor projects\Web\backend"
# 复制模板文件
copy .env.template .env
# 编辑.env文件，填入你的API密钥
notepad .env
```

### 方法2：手动创建

1. 打开 `backend` 文件夹
2. 创建新文件，命名为 `.env`（注意前面有点）
3. 复制以下内容并填入你的API密钥：

```ini
# AI模型提供商配置
AI_PROVIDER=deepseek

# DeepSeek（硅基流动）- 你当前的配置
AI_API_KEY=sk-hrrwzmmxydqkpitunkfonggkghnnxoshuakucxarfxczpmab
AI_API_BASE_URL=https://api.siliconflow.cn/v1
AI_MODEL=deepseek-ai/DeepSeek-V3

# Moonshot配置（填入你的真实密钥）
MOONSHOT_API_KEY=你的moonshot_api_key_here
MOONSHOT_API_BASE_URL=https://api.moonshot.cn/v1
MOONSHOT_MODEL=moonshot-v1-8k

# 其他模型配置（如果有的话）
WENXIN_API_KEY=你的文心一言密钥
XINGHUO_API_KEY=你的星火密钥
CHATGLM_API_KEY=你的ChatGLM密钥
```

## 🚀 第二步：启动后端服务

### 方法1：使用启动脚本（推荐）

```powershell
cd "F:\Cursor projects\Web\backend"
.\start.bat
```

### 方法2：手动启动

```powershell
cd "F:\Cursor projects\Web\backend"
# 激活虚拟环境
.\venv\Scripts\activate
# 启动服务
uvicorn main:app --reload --port 8000
```

## ✅ 启动成功的标志

后端启动成功后，你应该看到：

```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
✅ 数据库表创建成功
```

## 🔍 验证服务

### 1. 健康检查
访问：http://127.0.0.1:8000/health

应该返回：
```json
{"status": "ok", "message": "服务运行正常"}
```

### 2. API文档
访问：http://127.0.0.1:8000/docs

应该看到Swagger API文档界面

### 3. Providers接口
访问：http://127.0.0.1:8000/api/v1/ai/providers

应该返回：
```json
{
  "providers": ["deepseek", "wenxin", "xinghuo", "chatglm", "moonshot"],
  "current": "deepseek"
}
```

## ⚠️ 常见问题

### 问题1：找不到.env文件
**解决**：在 `backend` 目录下创建 `.env` 文件

### 问题2：端口被占用
**解决**：修改 `start.bat` 中的端口号，或关闭占用8000端口的程序

### 问题3：虚拟环境未激活
**解决**：确保运行了 `.\venv\Scripts\activate`

### 问题4：依赖未安装
**解决**：运行 `pip install -r requirements.txt`

---

**现在就可以启动后端了！** 🚀

