# 项目打包清单

## 📦 需要打包的文件和目录

### ✅ 必须包含的文件

#### 根目录文件
- `README.md` - 项目说明文档
- `DEVELOPMENT.md` - 开发文档
- `LICENSE` - 许可证文件
- `.gitignore` - Git 忽略规则
- `docker-compose.yml` - Docker 编排配置
- `API_KEY_SECURITY_CHECK.md` - API 密钥安全检查说明
- `GITHUB_PROFILE_README.md` - GitHub 配置文件

#### 后端文件 (backend/)
- `main.py` - FastAPI 应用入口
- `database.py` - 数据库配置
- `requirements.txt` - Python 依赖
- `Dockerfile` - Docker 镜像配置
- `.env.template` - 环境变量模板（重要！）
- `core/` - 核心模块目录（全部）
- `models/` - 数据模型目录（全部）
- `routers/` - API 路由目录（全部）
- `services/` - 业务逻辑层目录（全部）
- `repositories/` - 数据访问层目录（全部）
- `schemas/` - Pydantic 模型目录（全部）
- `utils/` - 工具函数目录（全部）
- `tests/` - 单元测试目录（全部）
- `seed_data/` - 种子数据目录（全部）
- `scripts/` - 脚本目录（全部）

#### 前端文件 (frontend/)
- `package.json` - Node.js 依赖配置
- `package-lock.json` - 依赖锁定文件
- `vite.config.js` - Vite 构建配置
- `tailwind.config.js` - TailwindCSS 配置
- `postcss.config.js` - PostCSS 配置
- `index.html` - HTML 入口文件
- `Dockerfile` - Docker 镜像配置
- `nginx.conf` - Nginx 配置
- `.env.development` - 前端开发环境变量模板
- `.gitignore` - 前端 Git 忽略规则
- `src/` - 源代码目录（全部）

### ❌ 必须排除的文件和目录

#### 依赖和构建产物
- `node_modules/` - Node.js 依赖（需要重新安装）
- `frontend/dist/` - 前端构建产物
- `backend/dist/` - 后端构建产物
- `build/` - 构建目录

#### Python 相关
- `__pycache__/` - Python 缓存
- `*.pyc`, `*.pyo`, `*.pyd` - Python 编译文件
- `venv/`, `.venv/`, `env/`, `ENV/` - 虚拟环境
- `*.egg-info/` - Python 包信息

#### 数据库和日志
- `*.db`, `*.sqlite`, `*.sqlite3` - 数据库文件
- `*.db-journal` - 数据库日志
- `logs/` - 日志目录
- `*.log` - 日志文件

#### 环境变量和敏感信息
- `.env` - 实际环境变量文件（不要打包！）
- `.env.local` - 本地环境变量
- `backend/uploads/` - 上传文件目录
- `backend/reports/` - 生成的报告目录

#### IDE 和编辑器
- `.vscode/` - VS Code 配置
- `.idea/` - IntelliJ IDEA 配置
- `*.swp`, `*.swo` - Vim 临时文件

#### 测试和缓存
- `.pytest_cache/` - pytest 缓存
- `.coverage` - 测试覆盖率
- `htmlcov/` - 测试覆盖率报告
- `.eslintcache` - ESLint 缓存

#### 临时文件
- `*.tmp`, `*.temp` - 临时文件
- `*.bak`, `*.backup` - 备份文件
- `.DS_Store` - macOS 系统文件

#### 文档（可选排除，建议保留）
以下文档可以根据需要选择是否包含：
- `GitHub上传完整指南.md`
- `GITHUB_UPLOAD_CHECKLIST.md`
- `项目整理说明.md`
- `上传到GitHub的步骤.md`
- `backend/*.md` - 后端临时文档（根据 .gitignore 规则）

## 📋 打包后检查清单

打包完成后，请确认：

- [ ] 包含 `backend/.env.template` 文件
- [ ] 包含 `frontend/.env.development` 文件
- [ ] 包含 `requirements.txt` 和 `package.json`
- [ ] 包含所有源代码目录（core/, models/, routers/, services/ 等）
- [ ] 不包含 `.env` 实际环境变量文件
- [ ] 不包含 `node_modules/` 目录
- [ ] 不包含 `__pycache__/` 目录
- [ ] 不包含数据库文件（*.db, *.sqlite）
- [ ] 不包含虚拟环境目录（venv/, .venv/）
- [ ] 不包含构建产物（dist/, build/）

## 🚀 团队成员接收后的操作步骤

1. **解压项目文件**
   ```bash
   unzip Web.zip  # 或使用其他解压工具
   cd Web
   ```

2. **配置后端环境**
   ```bash
   cd backend
   # 复制环境变量模板
   cp .env.template .env
   # 编辑 .env 文件，填入配置
   # 创建虚拟环境
   python -m venv venv
   # 激活虚拟环境
   # Windows: venv\Scripts\activate
   # Linux/Mac: source venv/bin/activate
   # 安装依赖
   pip install -r requirements.txt
   ```

3. **配置前端环境**
   ```bash
   cd frontend
   # 安装依赖
   npm install
   # 检查 .env.development 文件是否存在
   ```

4. **启动项目**
   ```bash
   # 后端（在 backend 目录）
   uvicorn main:app --reload --port 8000
   
   # 前端（在 frontend 目录，新终端）
   npm run dev
   ```

## 📝 注意事项

1. **环境变量**：团队成员需要根据 `.env.template` 创建自己的 `.env` 文件
2. **API 密钥**：不要将包含真实 API 密钥的 `.env` 文件打包
3. **数据库**：每个成员应该使用自己的本地数据库
4. **依赖安装**：确保团队成员安装了 Python 3.10+ 和 Node.js 18+
5. **端口冲突**：确保 8000（后端）和 5173（前端）端口未被占用
