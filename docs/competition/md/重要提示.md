# ⚠️ 重要提示

## 当前状态

你**已在新的终端窗口运行** `npm run dev`，这说明 Node.js 已经安装！

但是遇到了错误：
```
'vite' is not recognized as an internal or external command
```

这是因为**依赖包还没有安装**。

## ✅ 解决方案

### 在**同一个终端窗口**中执行：

```powershell
# 1. 进入前端目录（使用引号包裹路径）
cd "F:\Cursor projects\Web\frontend"

# 2. 安装依赖（首次运行必须执行）
npm install

# 3. 等待安装完成（大约 1-2 分钟）

# 4. 启动开发服务器
npm run dev
```

## 📋 完整流程

```powershell
# 进入项目目录
cd "F:\Cursor projects\Web\frontend"

# 安装依赖（首次运行）
npm install

# 安装完成后会显示很多绿色提示

# 启动服务
npm run dev
```

## 🎯 预期结果

启动成功后会显示：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

然后访问：http://localhost:5173

## ⚙️ 需要同时运行

1. **后端服务**（在另一个终端窗口）：
   ```powershell
   cd "F:\Cursor projects\Web\backend"
   .\start.bat
   ```

2. **前端服务**（在另一个终端窗口）：
   ```powershell
   cd "F:\Cursor projects\Web\frontend"
   npm install    # 只需运行一次
   npm run dev    # 每次启动
   ```

## 🌐 访问地址

- **前端**：http://localhost:5173
- **后端**：http://127.0.0.1:8000
- **后端API文档**：http://127.0.0.1:8000/docs

## 📝 注意事项

1. 确保在两个**不同的**终端窗口分别运行前后端
2. 前端依赖只需安装一次（`npm install`）
3. 每次开发时只需运行 `npm run dev`

