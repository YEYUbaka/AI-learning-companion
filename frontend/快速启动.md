# 🚀 前端快速启动指南

## ✅ 项目已创建完成

完整的 React + Vite + TailwindCSS 前端项目已搭建完毕！

## 📦 启动步骤

### 1. 进入项目目录
```bash
cd frontend
```

### 2. 安装依赖（首次运行）
```bash
npm install
```

### 3. 启动开发服务器
```bash
npm run dev
```

### 4. 访问应用
```
http://localhost:5173
```

## 🎯 页面路由

- **登录页**: http://localhost:5173/login
- **注册页**: http://localhost:5173/register
- **首页**: http://localhost:5173/dashboard
- **AI问答**: http://localhost:5173/ai

## ⚙️ 重要提示

1. **确保后端已启动**
   - 后端地址：http://127.0.0.1:8000
   - 在 `frontend/src/api/apiClient.js` 中配置

2. **测试流程**
   - 先访问 `/register` 注册账号
   - 返回 `/login` 登录
   - 进入 `/dashboard` 查看首页
   - 点击 "AI助手" 或访问 `/ai` 测试问答

3. **常见问题**
   - 端口被占用：修改 vite.config.js 中的端口
   - API 连接失败：检查后端是否启动
   - 模块导入错误：运行 `npm install`

## 📝 项目结构

所有文件已创建完成：
- ✅ 配置文件（package.json, vite.config.js 等）
- ✅ 页面组件（Login, Register, Dashboard, AIChat）
- ✅ 公共组件（Navbar）
- ✅ API 封装（apiClient.js）
- ✅ 样式文件（global.css）

现在直接运行 `npm install` 和 `npm run dev` 即可！

