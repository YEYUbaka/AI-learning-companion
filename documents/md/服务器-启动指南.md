# 智学伴项目 - 服务器启动指南

## 🔍 问题诊断

**错误信息：** `Fatal error in launcher: Unable to create process using "F:\Cursor projects\Web\backend\venv\Scripts\python.exe"`

**原因：** 虚拟环境是在 `F:\Cursor projects\Web\backend` 创建的，但项目现在在 `C:\Web\Zhixueban\backend`，导致路径不匹配。

---

## ✅ 解决方案：重新创建虚拟环境

### 方法一：使用自动修复脚本（推荐）

1. **运行修复脚本**
   ```powershell
   cd "C:\Web\Zhixueban\backend"
   .\服务器-修复虚拟环境.bat
   ```

2. **等待脚本完成**
   - 脚本会自动删除旧虚拟环境
   - 创建新的虚拟环境
   - 安装所有依赖

3. **启动服务**
   ```powershell
   .\venv\Scripts\activate
   uvicorn main:app --reload --port 8000
   ```

### 方法二：手动修复

#### 步骤1：删除旧虚拟环境
```powershell
cd "C:\Web\Zhixueban\backend"
Remove-Item -Recurse -Force venv -ErrorAction SilentlyContinue
```

#### 步骤2：创建新虚拟环境
```powershell
python -m venv venv
```

#### 步骤3：激活虚拟环境
```powershell
.\venv\Scripts\activate
```

#### 步骤4：升级 pip
```powershell
python -m pip install --upgrade pip
```

#### 步骤5：安装依赖
```powershell
pip install -r requirements.txt
```

#### 步骤6：验证安装
```powershell
python -c "import fastapi, uvicorn, reportlab; print('✅ 所有依赖已安装')"
```

---

## 🚀 完整启动命令

### 后端启动（PowerShell窗口1）

```powershell
# 进入后端目录
cd "C:\Web\Zhixueban\backend"

# 激活虚拟环境
.\venv\Scripts\activate

# 启动服务
uvicorn main:app --reload --port 8000
```

**或者使用 Python 模块方式：**
```powershell
cd "C:\Web\Zhixueban\backend"
.\venv\Scripts\activate
python -m uvicorn main:app --reload --port 8000
```

### 前端启动（PowerShell窗口2）

```powershell
# 进入前端目录
cd "C:\Web\Zhixueban\frontend"

# 启动服务
npm run dev
```

---

## 🔍 验证服务是否正常

### 1. 检查后端API
在浏览器中访问：
- http://127.0.0.1:8000/docs （Swagger文档）
- http://127.0.0.1:8000/health （健康检查）

### 2. 检查前端
在浏览器中访问：
- http://localhost:5173/ （或显示的端口）

---

## ⚠️ 常见问题

### 问题1：激活虚拟环境失败
**错误：** `无法加载文件，因为在此系统上禁止运行脚本`

**解决：**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 问题2：pip 安装失败
**错误：** `pip install` 报错

**解决：**
```powershell
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题3：端口被占用
**错误：** `Address already in use`

**解决：**
```powershell
# 使用其他端口
uvicorn main:app --reload --port 8001
```

### 问题4：找不到 requirements.txt
**错误：** `No such file or directory: requirements.txt`

**解决：**
- 确保在 `C:\Web\Zhixueban\backend` 目录中
- 检查文件是否存在：`dir requirements.txt`

---

## 📝 快速启动命令（一键复制）

### 后端启动
```powershell
cd "C:\Web\Zhixueban\backend"
.\venv\Scripts\activate
uvicorn main:app --reload --port 8000
```

### 前端启动
```powershell
cd "C:\Web\Zhixueban\frontend"
npm run dev
```

---

## ✅ 启动成功标志

### 后端成功标志：
```
INFO:     Will watch for changes in these directories: ['C:\\Web\\Zhixueban\\backend']
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxx] using WatchFiles
INFO:     Started server process [xxxx]
INFO:     Waiting for application startup.
✅ 数据库表创建成功
INFO:     Application startup complete.
```

### 前端成功标志：
```
> zhixueban-frontend@1.0.0 dev
> vite

  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

---

## 🔧 如果还是有问题

### 1. 检查 Python 版本
```powershell
python --version
# 应该是 Python 3.9 或更高版本
```

### 2. 检查虚拟环境是否正确创建
```powershell
cd "C:\Web\Zhixueban\backend"
.\venv\Scripts\python.exe --version
# 应该显示 Python 版本
```

### 3. 检查依赖是否安装
```powershell
.\venv\Scripts\activate
pip list | Select-String "fastapi|uvicorn|reportlab"
```

### 4. 完全重新安装
```powershell
cd "C:\Web\Zhixueban\backend"
Remove-Item -Recurse -Force venv
python -m venv venv
.\venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 📞 测试步骤

1. **修复虚拟环境**
   - 运行 `服务器-修复虚拟环境.bat` 或手动执行上述步骤

2. **启动后端**
   - 在 PowerShell 中运行后端启动命令
   - 等待看到 `✅ 数据库表创建成功`

3. **启动前端**
   - 在新的 PowerShell 窗口中运行前端启动命令
   - 等待看到 Vite 启动信息

4. **测试功能**
   - 访问前端页面
   - 登录系统
   - 进入 Dashboard
   - 测试 PDF 导出功能

---

## 💡 提示

- 如果虚拟环境路径问题持续存在，建议完全删除 `venv` 文件夹并重新创建
- 确保在正确的目录中运行命令（`C:\Web\Zhixueban\backend`）
- 如果使用服务器，可能需要使用 `--host 0.0.0.0` 参数来允许外部访问

