# 项目结构说明

## 整理后的目录结构

```
Web/
├── backend/              # 后端服务
│   ├── core/            # 核心模块（配置、安全、日志）
│   ├── models/          # 数据模型
│   ├── routers/         # API 路由
│   ├── services/        # 业务逻辑层
│   ├── repositories/    # 数据访问层
│   ├── schemas/         # Pydantic 模型
│   ├── utils/           # 工具函数
│   ├── tests/           # 单元测试
│   ├── seed_data/       # 种子数据
│   ├── uploads/         # 上传文件
│   ├── reports/         # 生成的报告
│   ├── main.py          # 应用入口
│   ├── database.py      # 数据库配置
│   └── requirements.txt # Python 依赖
│
├── frontend/            # 前端应用
│   ├── src/
│   │   ├── pages/      # 页面组件
│   │   ├── components/ # 通用组件
│   │   ├── api/        # API 客户端
│   │   ├── store/      # 状态管理
│   │   └── utils/      # 工具函数
│   ├── package.json    # Node.js 依赖
│   └── vite.config.js  # Vite 配置
│
├── scripts/             # 脚本目录
│   ├── setup/          # 环境安装和配置
│   ├── start/          # 服务启动
│   ├── deploy/         # 部署脚本
│   └── maintenance/    # 维护和修复
│
├── docs/                # 文档目录
│   ├── development/    # 开发文档
│   ├── deployment/     # 部署文档
│   ├── troubleshooting/# 故障排查
│   ├── competition/    # 比赛相关
│   └── architecture.md # 架构文档
│
├── _archive/           # 归档目录
│   ├── old_scripts/    # 旧脚本备份
│   └── temp_files/     # 临时文件
│
├── .cursor/            # Cursor 配置
├── .venv/              # Python 虚拟环境
├── logs/               # 日志目录
│
├── README.md           # 项目说明
├── CLAUDE.md           # Claude Code 指南
├── LICENSE             # 许可证
├── .gitignore          # Git 忽略文件
├── docker-compose.yml  # Docker 配置
└── 创建数据库.sql      # 数据库初始化脚本
```

## 快速导航

### 开发相关
- 项目说明：`README.md`
- 开发指南：`CLAUDE.md`
- 后端文档：`backend/README.md`
- 前端文档：`frontend/README.md`
- 详细开发文档：`docs/development/`

### 启动项目
- 一键启动：`scripts/start/5-一键启动前后端.bat`
- 启动后端：`scripts/start/2-启动后端服务.bat`
- 启动前端：`scripts/start/3-启动前端服务.bat`

### 环境配置
- 环境检查：`scripts/setup/1-完整部署检查.bat`
- 安装依赖：`scripts/setup/安装缺失依赖.bat`
- 创建数据库：`scripts/setup/创建数据库.bat`

### 问题排查
- 故障排查文档：`docs/troubleshooting/`
- 维护脚本：`scripts/maintenance/`

### 部署相关
- 部署文档：`docs/deployment/`
- 部署脚本：`scripts/deploy/`

## 整理说明

本次整理完成了以下工作：

1. **清理根目录**：只保留核心配置文件和说明文档
2. **统一脚本管理**：所有批处理文件按功能分类到 `scripts/` 目录
3. **统一文档管理**：所有文档按类型分类到 `docs/` 目录
4. **归档临时文件**：测试文件和旧脚本移至 `_archive/` 目录
5. **合并重复目录**：、`部署脚本/`、`documents/` 等重复目录

## 注意事项

- `_archive/` 目录中的文件为备份，可根据需要删除
- 所有脚本路径已更新，建议测试后使用
- 文档已分类整理，便于查找和维护
